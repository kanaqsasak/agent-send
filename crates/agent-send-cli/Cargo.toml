[package]
name = "agent-send-cli"
description = "Command-line client for agent-send"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
agent-send-core = { path = "../agent-send-core" }
agent-send-daemon = { path = "../agent-send-daemon" }
serde_json = "1"
sha2.workspace = true
